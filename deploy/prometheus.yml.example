global:
  scrape_interval: 15s

scrape_configs:
  - job_name: aegis
    scheme: http
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/aegis_metrics_token
    static_configs:
      - targets:
          - dashboard:5001
